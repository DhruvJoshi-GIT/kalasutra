# progress.md — handoff file for the Swadesh / KalaSutra hackathon build

> ## RESUME HERE — read this block first, then act as if you had done all of it yourself
>
> You are the engineer-designer on **Dhruv's hackathon entry**: an AI-driven marketplace + seller app for marginalised Indian artisans (problem statement in `plan.md`). You work in **`C:/swadesh/`** on Windows 11. The build is a **Python FastAPI + PostgreSQL backend in `backend/`** and a **plain-JS frontend in `web/`** (the approved prototype split into classic `<script>` files). Local stack: portable PostgreSQL 17 at `C:/swadesh/tools/pgsql` (data `C:/swadesh/pgdata`), backend venv at `backend/.venv`; start both with the commands under **How to run** if http://127.0.0.1:8000/api/health does not answer. The old Next.js app in `C:/swadesh/pickindian` is dropped and git-ignored (reference only). Node 24 exists at `C:/Program Files/nodejs` (used only for `node -e` syntax checks).
>
> **Standing rules from the user (never break these):**
> 1. **Never write to agent memory.** This folder is the only knowledge base; log every significant step here, and record decisions in `decisions.md`.
> 2. **Never publish Artifacts.** Show every design or page as a local HTML file or on localhost / the live site.
> 3. **Git**: the public repo is `https://github.com/DhruvJoshi-GIT/kalasutra` (root = `C:/swadesh`, branch `main`); commit as the user only (`git config` is set locally, no co-author trailer). Since 2026-09-08 the user wants **every change committed and pushed as it is made** ("make the changes simultaneously to the GitHub repo as well"); before that the rule was "push only on 'publish'". Never push to the `pickindian` remote. Nothing in `tools/ pgdata/ pickindian/ scratch/ *.log .env` is ever committed.
> 4. Build order the user chose: **website → AI features → Android app.** Sarvam AI is the language provider (1,000 free credits, fixture mode during UI work); Claude does structured JSON, vision QC and pricing reasoning; fal.ai / Voyage for cutout / embeddings — all behind fixture / record / live modes.
> 5. Design: direction **B "Karkhana" — ink on paper**, minimal variant (photo-first tiles, hairlines, one marigold accent, rounded corners), with the PrithviNet interaction language (hard offset shadows that lift on hover and press flat, circle-reveal theme toggle). **Brand (2026-09-08)**: the flame-क mark (`web/img/logo-mark.png`) and the bilingual wordmark **कलाSutra** (Hindi first half, English second) everywhere the name appears; header shows mark + name at the top of the page and collapses to the mark on scroll.
>
> **Where you are right now (2026-09-09, late):** the site is **live at http://kalasutra.live** (GitHub Pages, custom domain; root `index.html` redirects to `web/`). HTTPS is **enforced since 2026-09-09** (the custom domain was removed and re-added through the Pages API, GitHub issued the certificate within minutes — valid to 2026-12-08 — and `https_enforced` was switched on; `http://` now 301s to `https://`). **The backend is not deployed** (Render + Neon accounts still to be created by the user; `render.yaml` at the repo root, `FAL_KEY` has its spot there). Until then the public site runs in **demo mode** (`web/js/demo.js`). Today's batch (all pushed): the user's new photos are in (82 listings, 14 makers, new *pottery* category), the opening slider is seven category banners shown whole (never cropped), every screen was fixed for phones down to 280 px, the bug that stopped any product from opening when signed out is fixed (`session.user` on a null session), and the **AI photo studio** for sellers is built and working on the live site with no key: photo → on-device cutout → choice of eight backdrops → listing. Phase 2 server routes for sellers exist now too (uploads, files, own products) and the studio's server tier is wired to fal.ai behind `FAL_KEY`.
>
> **The one step left for the user on the studio:** paste a fal.ai key into `FAL_KEY` (Render env var, or `backend/.env` locally). Nothing else changes: `GET /api/ai/studio` then reports `server:true` and the browser sends photos to the server instead of running the on-device model. Without the key everything still works on-device.
>
> **Your very next move, in order:** (1) if the user says Render + Neon exist: fill `DATABASE_URL`, `SARVAM_API_KEY`, `FAL_KEY` (optional) in Render → Blueprint → `GET /api/health` → the live site leaves demo mode by itself; (2) next build step = **voice cataloguer + pricing in fixture mode** (phase 4 in `plan.md`; the seller screen already has the shape: `doDescribe()` in `web/js/seller.js` uses canned copy per category until Sarvam + Claude are wired). Regenerate `web/catalogue.json` with `python backend/scripts/snapshot.py` after any seed change and run `scratch/shoot.py` before pushing layout changes.
>
> Read the rest of this file, then `decisions.md` (why things are the way they are), `api-notes.md` (verified shapes) and `plan.md` before touching code. Talk to the user plainly and briefly; they review by clicking through on the live site and their phone, not by reading reports.

> **If you are a fresh agent: read this file top to bottom, then `decisions.md`, `api-notes.md` and `plan.md` (`findings.md` is the audit of the dropped Next.js app). Do not write anything to agent memory — this folder is the only knowledge base. Do not publish Artifacts. Update this file after every significant execution and push.**

## What this is

Hackathon build: *AI-Driven Market Linkage and Smart Cataloging Mobile Application for Marginalized Artisans*. Real build: FastAPI + Postgres in `backend/`, plain-JS frontend in `web/` (from the approved prototype), live at http://kalasutra.live. The user's private Next.js repo `InfinitelyAsymptotic/pickindian` (clone at `C:/swadesh/pickindian`) was the first base and is now reference only. Full plan in `plan.md`; repo audit in `findings.md`; rationale for every decision in `decisions.md`; endpoint shapes in `api-notes.md`.

**Build order (user's call): 1. website → 2. key AI features → 3. Android app.**

## Current phase

**Phase 2 (seller side) mostly done + the photo studio; site live on the domain in demo mode; backend not yet deployed.** (2026-09-09)

State of play:
- http://kalasutra.live → `web/` on GitHub Pages: 82 listings with the new photos, seven-banner slider, phone layouts down to 280 px, demo-mode sign-in / cart / orders / seller listings when the API is unreachable, and the photo studio for sellers (on-device cutout).
- Backend (`backend/`, FastAPI + Postgres) runs locally on :8000 with 16 tests green (incl. studio, uploads, seller products); Render + Neon deploy waits on the user's accounts.
- Everything is committed and pushed (`main`).
## Status board

| Item | State |
|---|---|
| Knowledge base (`C:/swadesh/*.md`) | ✅ |
| Repo cloned to `C:/swadesh/pickindian` | ✅ |
| Node.js LTS | ✅ v24.19.0 via winget (`C:/Program Files/nodejs`) |
| PostgreSQL 17.11 (portable, `C:/swadesh/tools/pgsql`, data `C:/swadesh/pgdata`) | ✅ running on :5432, user `postgres` / `postgres` |
| `npm install`, `.env`, `db:push`, `db:seed` | ✅ |
| Buyer storefront | ✅ home, listing, product page, cart, checkout (checkout is simulated — pre-existing) |
| `next.config.ts` image hosts (pre-existing bug) | ✅ fixed |
| Schema extended (ARTISAN/BUYER roles, ArtisanProfile, AI fields on Product/ProductImage, VoiceNote, PriceSuggestion, Enquiry, OtpChallenge, AiCache) | ✅ pushed |
| Role in NextAuth JWT/session + `/artisan/*` middleware guard | ✅ |
| Local file storage + `POST /api/upload` + `GET /api/files/<key>` | ✅ |
| Artisan APIs: profile (GET/POST/PUT), products (list/create/get/patch/delete), orders (list/patch status) | ✅ smoke-tested via curl as `priya@kalasetu.in` |
| `/sell` onboarding page | ✅ renders in the new design |
| `/artisan` portal: dashboard, products, new/edit product form, orders, profile + KYC, enquiries placeholder | ✅ renders in the new design |
| Seed: 3 artisans (Priya/Bagru VERIFIED, Meera/Madhubani SUBMITTED, Ramesh/Moradabad PENDING) × 3 products each + 4 demo orders | ✅ |
| Design system in code (`globals.css` tokens light + dark, `.neo` interactions, fonts) | ✅ |
| Design direction B applied to: home, header, footer, product card, listing, `/sell`, portal shell, dashboard, product form | ✅ |
| Pages wearing new tokens but still old layout: product detail, cart, checkout, account/*, login/signup, `ProductFilters`, portal products/orders/profile lists | ⬜ second pass pending |
| Hand-drawn vision prototype (`C:/swadesh/prototype/kalasutra-prototype.html`) | ✅ built · ⬜ **user review pending** |
| Restructure live site to the hand-drawn IA (sidebar-first, P/C/W/S, upload checklist) | ⬜ after review |
| AI: image studio / voice cataloguer / pricing | ⬜ phase 2 — three disabled tool cells on the product form (`data-ai-hook`) |
| Dataset bootstrap (~500 flagged-synthetic SKUs) | ⬜ phase 2 |
| B2B enquiry + GeM CSV export | ⬜ later (enquiries page is an empty state) |
| Buyer checkout actually creating orders (pre-existing gap) | ⬜ later |
| Android app (Expo) | ⬜ phase 3 |
| Git: public repo `DhruvJoshi-GIT/kalasutra` (prototype, docs, photos, **backend/, web/**) | ✅ pushed 2026-09-05 evening |
| Backend phase 0 (models, Alembic, auth, catalogue, seed, tests) | ✅ |
| Backend phase 1 (cart, wishlist, addresses, payments, orders, reviews, comments, enquiries) | ✅ 11 tests green |
| `web/` frontend wired to the API (guest merge, checkout, account, login tabs) | ✅ verified in headless Edge |
| Phase 2: uploads (Postgres bytea), `GET /files`, seller products create/list/delete | ✅ 2026-09-09 · onboarding form, product edit, artisan orders ⬜ |
| AI photo studio (sellers): on-device cutout + 8 backdrops + shadow; server tier via fal.ai (`FAL_KEY`) | ✅ 2026-09-09 (key ⬜ user) |
| New photo batch: 82 listings, 14 makers, pottery category, thumbnails, banner slider | ✅ 2026-09-09 |
| Phone / tablet / watch / ultrawide layouts verified by `scratch/shoot.py` (140 shots, 0 overflow, 0 JS errors) | ✅ 2026-09-09 |
| AI pipelines: enhance ✅ (sync, no jobs needed) · catalog / price in fixture mode + jobs | ⬜ next |
| Deploy: Render (API) + Neon (DB) via `render.yaml`; keep-warm ping | ⬜ user to create accounts |
| Domain kalasutra.live -> GitHub Pages | ✅ DNS live 2026-09-08; root redirects to `web/`; **HTTPS enforced 2026-09-09** |
| Demo mode (`web/js/demo.js`): full buyer + seller flows without the API | ✅ 2026-09-08 |
| Vercel deployment | ⬜ user to delete the project in the Vercel dashboard (`vercel.json` removed from the repo) |
| Brand logo (flame क mark + bilingual कलाSutra wordmark) in header, favicon, footer | ✅ 2026-09-08 |
| Phone bottom-bar Categories sheet (was dead) + per-category counts | ✅ 2026-09-08 |
| Keys: Sarvam ✅ (user) · **fal.ai `FAL_KEY` ⬜ (the one step for the studio server tier)** · Anthropic / Voyage ⬜ |

## What works end-to-end today

1. **Live site** http://kalasutra.live (redirects to `/web/`): browse 25 products / 7 categories / 11 makers, search, sort, wishlist, product popup with reviews and comments, cart, checkout, account, maker pages, theme toggle; phone layout with bottom bar and Categories sheet.
2. **Demo mode** (no API): seller login with `9811000001` (Priya) / `9811000002` (Meera) + code `123456` → `#seller` shows their listings → `#upload` demo flow; buyer `demo@kalasutra.in` / `password123` or any new email → address → UPI/card → order → confirmed → Your orders.
3. **Local full stack**: Postgres + uvicorn on :8000 serving `web/` and the API (`/api/docs`); the same flows hit the real database; `pytest` green.
4. `C:/swadesh/prototype/kalasutra-prototype.html` — the frozen approved prototype (reference).
## Exact next action

1. **User-side blockers** (check when they say so): Name.com DNS ✅ · GitHub Pages HTTPS ✅ (enforced 2026-09-09) · Render + Neon accounts ⬜ · delete the Vercel project ⬜ · `FAL_KEY` (studio server tier) ⬜ · Anthropic / Voyage keys ⬜.
2. **Phase 2 — artisan side** (`plan.md` §D, §H): `POST/GET/PUT /artisan/profile`, `GET/POST/PATCH/DELETE /artisan/products`, `POST /uploads` + `GET /files/{key}` (Postgres bytea), `GET/PATCH /artisan/orders`; wire `#seller`, `#upload`, `#artist/<slug>` to them, keeping `demo.js` answers for the same routes so the public site still demos without the server.
3. **Phase 4 — AI in fixture mode**: `provider.py`, `job` table + polling, the three pipelines with `default.json` fixtures, `audio.js`, synthetic catalogue + kNN; `verify_sarvam.py` once with the real key.
4. Deploy (phase 3) as soon as the user has Render + Neon: Blueprint → set `DATABASE_URL`, `SARVAM_API_KEY`, `JWT_SECRET` → seed → `GET /api/health` → the live site leaves demo mode by itself.
## How to run

```powershell
# 0. Backend (FastAPI) — serves web/ at http://127.0.0.1:8000 and the API at /api (docs at /api/docs)
C:\swadesh\tools\pgsql\bin\pg_ctl -D C:\swadesh\pgdata -l C:\swadesh\pg.log start
cd C:\swadesh\backend; .venv\Scripts\python -m uvicorn app.main:app --port 8000
.venv\Scripts\python -m pytest -q tests          # 11 tests, uses DB kalasutra_test
.venv\Scripts\python scripts\seed.py            # idempotent re-seed of DB kalasutra
.venv\Scripts\alembic revision --autogenerate -m "..."; .venv\Scripts\alembic upgrade head
.venv\Scripts\python scripts\snapshot.py         # regenerates web/catalogue.json (demo-mode snapshot) from data/seed_products.json — no DB needed
# Testing the site (Playwright on the installed Edge; run from C:\swadesh; scratch/ is git-ignored):
backend\.venv\Scripts\python scratch\shoot.py scratch\shots\run           # every screen x 7 viewports; flags overflow + JS errors
backend\.venv\Scripts\python scratch\live_click.py http://kalasutra.live/web/  # real clicks on the live site (or `local`)
backend\.venv\Scripts\python scratch\studio_e2e.py                          # seller login -> studio -> listing, desk + phone
# Logins: buyer demo@kalasutra.in / password123 · makers by OTP: 9811000001 (Priya) … code 123456
```

Old Next.js app (kept only for reference, not part of the build any more):
```powershell
# 1. Postgres (portable) — once per boot
C:\swadesh\tools\pgsql\bin\pg_ctl -D C:\swadesh\pgdata -l C:\swadesh\pg.log start
# 2. App
cd C:\swadesh\pickindian; npm run dev        # http://localhost:3000
# Useful
npm run db:seed                              # wipes + reseeds everything
npx prisma studio                            # DB browser
npx tsc --noEmit; npx eslint src             # checks (pre-existing lint errors in product-filters.tsx / cart-drawer.tsx are out of scope)
```

Logins (all `password123`): buyer `demo@pickindian.com` · artisans `priya@kalasetu.in`, `meera@kalasetu.in`, `ramesh@kalasetu.in`.

**Memory note:** on 2026-09-04 Windows killed the dev server and Postgres for low memory. If localhost stops answering, run the two start commands above.

## Design system (in code)

- Direction **B — "Karkhana", ink on paper** (user's pick, "absolutely love the design"). Paper `#F6F5F0`, ink `#111111`, one accent marigold `#E2A100` (only for the next action), hairlines 1px / structure 2px, no radius, no soft shadows. Dark mode inverts ink/paper, marigold stays.
- Type: Archivo 900 display (uppercase, tight), Archivo 500/700 body, IBM Plex Mono labels/IDs, Noto Sans Devanagari for Hindi — all via `next/font` in `src/app/layout.tsx`. Utilities `.display` `.label` `.mono` `.hi` in `src/app/globals.css`.
- Interactions (from the user's repo `DhruvJoshi-GIT/PrithviNet`, behaviour only): `.neo` hard 3px offset shadow → 4px + `translate(-1,-1)` on hover → flat + `translate(3,3)` on press; `.neo-card` lifts to 5px; `.neo-press` nudges 2px; inputs grow a marigold offset shadow on focus; theme toggle (`src/components/ui/theme-toggle.tsx`, `src/components/providers/theme-provider.tsx`) uses `document.startViewTransition` with a circle reveal from bottom-right; `localStorage['ks-theme']`; anti-flash script in `<head>`.
- Files rewritten for the system: `globals.css`, `layout.tsx`, `components/ui/{button,card,input,select,textarea,badge,avatar}.tsx`, `components/layout/{header,footer}.tsx`, `components/products/product-card.tsx`, `app/page.tsx`, `app/(shop)/products/page.tsx`, `app/(shop)/sell/page.tsx`, `app/(artisan)/artisan/{layout,page}.tsx`, `app/(artisan)/artisan/enquiries/page.tsx`, `components/artisan/product-form.tsx`. Every other file had `rounded-*`/`shadow-*` classes stripped and was reformatted with Prettier (`--no-semi --single-quote --trailing-comma es5 --print-width 100`).
- Design working files (kept for reference; the Artifact canvases were deleted by the user and must not be republished): `C:/swadesh/design/directions/*.dc.html` + `canvas.json`.

## Hand-drawn vision → prototype

- Source: `C:/Users/Dhruv/Downloads/Untitled-2026-09-03-2216.excalidraw`; rasterised screens in `C:/swadesh/scratch/sketch/*.png` (re-render with the Pillow script if needed).
- Screens in the sketch: hero (double frame, centred block, "Scroll"); home (On sale / Recommended, W → scrollable wishlist panel, S → Contact box); listing (breadcrumb x › y › z + sort box, 5-wide grid); product detail (Related 2×2, right column: image, price, details, buy → / cart); seller upload (photo → AI description + Edit → price approx → **submit only when the checklist is OK**; checklist Photo/Description/Price); artist portfolio (portrait, NAME, bio, work grid; story in English/Hindi translated by LLM + regional language given by seller, toggle); seller login (form + "motivational auraat" illustration promoting sellers); seller home (same chrome + breadcrumb + **+** add); cart (rows + "List of items" Name/Price → arrow); order confirmed (tick, "Order Confirmed", धन्यवाद); profile (Address / Payment / Your orders, avatar, details box).
- Shared chrome: square logo top-left, search bar, icon cluster **P C W S**, ↑ button, left category sidebar with three filter dots.
- Prototype: `C:/swadesh/prototype/kalasutra-prototype.html` (+ `prototype/img/*.jpg`, the cropped product photos; `photo/1.png`, `photo/2.png` are the user's source collages) — hash-routed, ink-on-paper system, PrithviNet interactions, working upload checklist (Upload → Describe → price → Submit unlocks), EN⇄हिन्दी story toggle, theme toggle, screen-map bar at the bottom. Verified: every screen template renders (node dry run), no syntax errors.

## Env vars (`backend/.env`, see `backend/.env.example`)

| Var | Status |
|---|---|
| `DATABASE_URL` | ✅ local `postgresql://postgres:postgres@localhost:5432/kalasutra` (tests use `kalasutra_test`) |
| `JWT_SECRET` | ✅ local value |
| `OTP_DEV_MODE=1`, `OTP_DEV_CODE=123456` | ✅ |
| `CORS_ORIGINS` | localhost + `https://dhruvjoshi-git.github.io` + `https://kalasutra.live` |
| `AI_MODE` | `fixture` |
| `SARVAM_API_KEY` | ✅ pasted by the user (not committed) |
| `ANTHROPIC_API_KEY`, `VOYAGE_API_KEY`, `FAL_KEY` | ⬜ user to provide (every route runs in fixture mode without them) |
| `ADMIN_KEY`, `SERVE_WEB` | local only |
## Photo studio: the one step left

Paste a fal.ai key (https://fal.ai/dashboard/keys) into `FAL_KEY` — in Render (env var, already declared in `render.yaml`) or in `backend/.env` locally. That switches the studio to the server tier (BiRefNet); leave it empty and the studio runs on-device in the browser. First live call: run the request in `backend/app/services/ai/fal.py` once and confirm the field names (`api-notes.md`).

## Sarvam credits remaining

1,000 (none used yet).

## Open questions for the user

- Render + Neon: when created, paste `DATABASE_URL` into Render and click New → Blueprint on this repo.
- Vercel: delete the old project in the Vercel dashboard (Project → Settings → Delete).
- API keys: Anthropic, Voyage, fal — needed only to leave fixture mode.
- Buyer↔seller comms: recommendation stands — enquiry → quote as core; voice-reply thread and SMS/WhatsApp notifications as later differentiators.
## Changelog (newest first)

- **2026-09-09 (late — seller login on the live site fixed, pushed)** — User: seller login did not work on kalasutra.live, so none of the seller features could be reached. Reproduced through the real form with Playwright: after "Send code" the page reloaded as `?phone=…` and the code step never appeared. Cause: every form used `onsubmit="return fn(this)"` with an **async** handler — the returned Promise is truthy, so the browser also submitted the form natively. The earlier checks had called the API functions directly and missed it. Fix: `document.addEventListener('submit', e => e.preventDefault())` in `web/js/main.js` (no form on the site needs a native submit). Verified on the live domain via the real UI at 1366 px and 390 px: seller OTP `9811000001` / `123456` → `#seller` as Priya → `#upload` → studio (on-device cutout 29–37 s) → listing saved and reopened; buyer `demo@kalasutra.in` → address → UPI → Place order → `#confirmed/…` → review posted. Scripts: `scratch/seller_login.py`, `scratch/buyer_forms.py`.
- **2026-09-09 (photos, slider, phone layouts, click bug, photo studio, seller API — pushed)** — User's asks: put the new photo zips on the site, stop the slider cropping on any device, un-cram every phone view, fix "nothing is clickable / no product opens", build the AI photo studio for sellers with only the API key left for them, and push every change. **Photos**: 8 zips in `photo/new photos/` (77 images) reviewed on contact sheets; 66 used → `web/img/*.jpg` (≤1400 px) + `web/img/t/*.jpg` thumbnails for every product; 6 existing listings re-photographed, 57 new listings, 3 new makers (`khurja`, `kundan`, `warli`, appended so demo OTP numbers stay), new `pottery` category (10 pieces); `backend/scripts/snapshot.py` now builds `web/catalogue.json` from the seed JSON without a DB. **Slider**: the seven `slider.zip` banners, edge-extended to exactly 3.2:1 in Pillow, shown whole in a box with the same ratio (`aspect-ratio` + `contain`), 900 px `srcset` for phones, full-bleed on phones, touch swipe, each a link to its category. **Click bug** (reproduced with Playwright on the live site): `productHtml` read `session.user` while signed out (`session` is null) → TypeError before the popup rendered; same in seller/upload/confirmed; every read is now `session?.user?.…`. **Phone layouts**: inline `grid-template-columns` on upload/artist/login beat the 900 px media query → `!important` stack rule; cart rows are a 2-row grid; popup buttons are a 2-column grid; tiny-screen rules at 340/290 px; toast wraps. **Photo studio** (`web/js/studio.js`, `#studio`, sellers only): take/upload → server cutout if `KS_STUDIO.server` (fal.ai via `POST /api/ai/cutout`) else on-device `@imgly/background-removal` (jsDelivr, 45 MB once) else framed fallback → alpha clean-up (solid-pixel mask, largest blob + ≥6 % blobs, dilate, crop) → 1024² canvas composite with 8 backdrops + contact shadow → "Use this photo" → `#upload` (category select, canned copy per category, price band) → `POST /artisan/products` with the JPEG data URL. **Backend**: `services/ai/{provider,fal,image}.py`, `services/storage.py`, `routers/ai.py` (`/ai/studio`, `/ai/cutout`, `/ai/enhance`), `routers/artisan.py` (`/uploads`, `/files/{key}`, `/artisan/products` GET/POST/DELETE), `schemas/artisan.py`; `FAL_KEY` spot in `render.yaml` + `.env.example`; 16 tests green. **Verified**: Playwright (`scratch/shoot.py`) 140 screenshots at 280/390/844×390/820/1366/1920/3440 px — 0 overflow, 0 JS errors; real-click test opens products on desktop + touch; studio e2e on desk + phone: on-device cutout 27–49 s, listing saved, survives reload, opens as a product. Repo moved to `C:\swadesh`; docs updated. Zips not committed (133 MB). HTTPS: custom domain re-added via the Pages API to retrigger the certificate.
- **2026-09-08 (demo mode — seller + buyer flows work without the server, pushed)** — User: "making a seller account … the demo thingy does not open up" on mobile and desktop. Cause: on kalasutra.live there is no API yet (Render not deployed), so `POST /auth/otp/request` failed with "Cannot reach the KalaSutra server" and the OTP step never appeared. Fix: **`web/js/demo.js`** — when the bootstrap fetch fails, `window.KS_OFFLINE=true` and `api()` routes every call to `demoApi()`, which simulates the backend locally (state in `localStorage['ks-demo']`): OTP request/verify (code `123456`; `98110000NN` maps to the Nth maker in the catalogue snapshot, other numbers get `needsProfile`), email login/register (demo buyer `demo@kalasutra.in` / `password123`, any other email creates a buyer), `/me`, cart + wishlist mirrors, addresses (6-digit PIN), payment labels (UPI id / card → `•••• 1234`), orders (server-side price rule, free shipping ≥ ₹999, `KS<base36>` numbers), reviews (one per name) and comments, enquiries; anything else throws "Not available in demo mode". A non-demo token is dropped when entering demo mode. Verified headlessly with `?api=` pointed at a dead port: Priya's OTP login → `#seller` shows her 6 listings (1400 px and 390 px), buyer sign-in → address → UPI → order → confirmed page. **Login screen on small screens** was two squeezed columns (inline grid on `.box.login` beat the stacking rule): new `@media (max-width:900px)` rules stack it, form first, illustration folded beneath (SVG hidden). Harness gained `api=`, `reset=1` and `js=` params (`scratch/shots/_shot.html`). When Render goes live nothing changes: the real API answers and demo mode never engages.
- **2026-09-08 (domain live — root points at web/, Vercel dropped, offline catalogue)** — User added the six DNS records at Name.com (verified: apex -> GitHub Pages IPs, `www` CNAME -> `dhruvjoshi-git.github.io`, `api` CNAME -> Render host). `http://kalasutra.live/` answers 200; the github.io URL 301s to it. **Root `index.html` now redirects to `web/`** (it used to open the old prototype, so the domain showed the wrong site); `vercel.json` removed — the user wants the Vercel deployment (`*.vercel.app`) gone; the project itself must be deleted in the Vercel dashboard (Project -> Settings -> Delete), which only the user can do. **Offline catalogue**: `web/catalogue.json` is a snapshot of `GET /api/catalogue/bootstrap` (25 products, 11 makers, 7 categories); `loadCatalogue()` falls back to it (with a toast, `window.KS_OFFLINE=true`) when the API is unreachable, so the public site shows the shop before Render is deployed — verified locally with `?api=` pointed at a dead port. Regenerate the snapshot after seed changes: `curl -s http://127.0.0.1:8000/api/catalogue/bootstrap` -> unwrap `data` -> `web/catalogue.json`. HTTPS on Pages: certificate not issued yet at the time of writing; `https_enforced` is flipped on via the Pages API as soon as it is.
- **2026-09-08 (logo + mobile categories, pushed)** — User supplied the brand logo (`C:/Users/Dhruv/Downloads/logo.jpeg`: a flame containing the Devanagari letter क, wordmark **कलाSutra** — Hindi first half, English second). The flame mark was cut out of the JPEG with Pillow into a transparent PNG (`web/img/logo-mark.png`, 512 px, ink on alpha; `web/img/favicon.png` 64 px, now the tab icon) and the wordmark is rendered as text (`<span class="hi">कला</span>Sutra`, Noto Sans Devanagari + Archivo) so it stays crisp and themeable. **Header**: the tile is now paper with the mark in ink (`.dark` inverts the PNG with `filter:invert(1)`); at the top of the page the name expands beside the mark, on scroll (>40 px) it collapses to the mark alone — same `.logo.wide` / `paintTop()` mechanism, now animating padding + gap + `max-width` + opacity. The name is bilingual everywhere it appears in the UI (`<title>`, footer ©, account page). **Mobile categories bug fixed** (user: tapping Categories in the bottom bar did nothing): `toggleCats()` re-rendered the bottom nav, so the tapped link was detached before the document click listener ran; that listener then treated the tap as "outside" and closed the sheet in the same tick. Fix: toggle the link's `on` class in place and skip the outside-click check when `e.target.isConnected` is false. The capsule/sheet also shows a **live listing count per category** (from the bootstrap catalogue, `.ct`), hidden when the capsule is collapsed. Verified with headless Edge at 1600 px (top / scrolled / dark) and 390 px (home, sheet open light + dark, scrolled header) — the 390 px frames are taken through an iframe harness (`scratch/shots/_shot.html`, must be copied into `web/` while shooting because Windows enforces a ~500 px minimum window width on headless Edge). **User's new instruction: push every change to GitHub as it is made** (supersedes "wait for publish" for this session's work). Local stack restarted: portable Postgres + uvicorn on :8000. DNS for kalasutra.live is still on Name.com parking as of today; the user has the domain and was given the exact records to add.
- **2026-09-05 (evening — domain + machine state)** — **Custom domain**: repo has `CNAME` = `kalasutra.live` and `.nojekyll`; GitHub Pages custom domain is set via the API (HTTPS enforcement pending until the certificate exists). **DNS at Name.com is NOT done yet**: the apex still resolves to Name.com parking (`91.195.240.94`) and `www` has no record. User must delete the parking A record, add the four GitHub Pages A records (185.199.108.153 / .109.153 / .110.153 / .111.153), add CNAME `www` -> `dhruvjoshi-git.github.io`, and switch off URL forwarding/parking. Then `PUT /repos/DhruvJoshi-GIT/kalasutra/pages {"cname":"kalasutra.live","https_enforced":true}` (retry until the cert is issued) and verify `https://kalasutra.live/`. The old github.io URL redirects to the domain. **Pushed** to GitHub this evening because the user asked to move the site to the domain; the push included the whole backend + `web/` + `render.yaml`. **Hosting**: Heroku failed -> Render (blueprint at repo root) + Neon Postgres; the user still has to create both accounts and click New -> Blueprint; nothing is deployed yet. **Sarvam key** is in `backend/.env`. **Machine**: Windows killed the portable Postgres and uvicorn for low memory (a game was using 7 GB); restart with the commands under *How to run*. Background watchers were killed too — do not poll; check once when the user says DNS is done.
- **2026-09-05 (hosting switch)** — User: Sarvam key is in `backend/.env`; **Heroku did not work** → hosting is now **Render** (free web service, `render.yaml` blueprint at repo root, rootDir `backend`, start = `alembic upgrade head && seed && uvicorn`, health `/api/health`) + **Neon** free Postgres (never expires, wakes in ms; `DATABASE_URL` pasted into Render). Frontend stays on GitHub Pages; `web/config.js` targets `kalasutra-api.onrender.com` from github.io and `api.kalasutra.live` from the custom domain (Render custom domain + TLS is free; Name.com CNAME `api` → the onrender host). Render free sleeps after 15 min idle → a cron-job.org ping every 10 min keeps it warm. User must: sign up at neon.tech and render.com with GitHub, create the Neon project, then Render → New → Blueprint → this repo → fill DATABASE_URL + SARVAM_API_KEY. Pushing the backend to GitHub (needed for the blueprint) waits for the user's "publish".
- **2026-09-05 (phase 1 done — buyer API + real frontend)** — Backend: `routers/buyer.py` (cart GET/PUT/DELETE + `/cart/merge` union-max, wishlist GET/POST/DELETE + merge, addresses, payment methods (label only, card number discarded), orders POST/GET with server-side prices, free shipping ≥ ₹999 else ₹79, simulated PAID, cart cleared), `routers/social.py` (reviews one-per-user, comments + artisan answer, B2B enquiries create/list/quote), `services/orders.py`; 11 tests green. Frontend: `web/` = the prototype split by a scripted splitter (`css/system.css` verbatim; `js/data,state,api,chrome,tiles,screens,modal,router,seller,main.js` as plain scripts loaded in order so every inline handler keeps working; `config.js` picks the API URL — `?api=` override → `ks-api`). Catalogue comes from `/api/catalogue/bootstrap` (cached in `ks-catalogue`, paints instantly); guests keep cart/wishlist in localStorage and they **merge on sign-in**; cart writes mirror to the server (debounced); addresses/payments/orders/reviews/comments are API-only; checkout shows an inline sign-in / create-account box for guests; login screen has Buyer (email) and Maker (phone OTP) tabs; account page has Sign out and PATCH /me; maker page "Request a bulk quote" creates an enquiry; the seller upload demo still runs locally (phase 2 wires it). Verified in headless Edge against the live API: popup with reviews/comments, guest cart, sign-in merge, address + UPI, order → account, OTP screen, 390px layout. Run: uvicorn serves `web/` at http://127.0.0.1:8000/. **Committed locally, not pushed.**
- **2026-09-05 (phase 0 done — backend skeleton)** — User said go; domain is **kalasutra.live** (Name.com), Sarvam key in hand (to be pasted by the user into `backend/.env` → `SARVAM_API_KEY=`). Built `backend/`: FastAPI app (`app/main.py`, CORS, `/api/health`, serves `web/` or `prototype/` at `/` in dev), settings (`app/config.py`, reads `backend/.env`), SQLAlchemy 2 models for every table (user_account, address, payment_method, artisan_profile, otp_challenge, category, product, product_image, cart, cart_item, wishlist_item, customer_order, order_item, review, product_comment, enquiry, voice_note, price_suggestion, ai_cache, job, stored_file), Alembic `c767af967738_initial`, JWT auth (register/login, phone OTP with dev code 123456, `/me`), catalogue routes (`/catalogue/bootstrap` in the prototype's exact shape, `/products` with filter/sort/search incl. Hindi, `/products/{id|slug}`, `/categories`, `/makers`, `/makers/{slug}`), `scripts/seed.py` (7 categories, 11 makers as OTP-login users `+91981100000N`, 25 products from `data/seed_products.json`, demo buyer `demo@kalasutra.in` / `password123`, admin), tests (7 passing on `kalasutra_test`). Local DBs `kalasutra` + `kalasutra_test` on the portable Postgres (which had crashed again and was restarted). Root `Procfile`, `requirements.txt`, `.python-version` for Heroku. Run: `cd backend && .venv/Scripts/python -m uvicorn app.main:app --port 8000` → http://127.0.0.1:8000/api/docs. **Not committed yet.**
- **2026-09-05 (v2.7 — minimal look published)** — User approved the minimal variant: it is now `prototype/kalasutra-prototype.html` (the boxed version is archived as `prototype/archive/kalasutra-prototype-v2-boxed-2026-09-05.html`). README rewritten to describe the minimal design system, the three AI features and the v2 architecture/hosting. Pushed on the user's word.
- **2026-09-05 (minimal variant, local only)** — User: show changes locally before publishing (new standing rule above). Built `prototype/kalasutra-minimal.html` — the same prototype with the minimalist pass applied via an override stylesheet: tiles without boxes (photo with rounded corners, caption on plain paper, outline only on hover), caption = name (regular) / craft · place (mono grey) / price (bold), no maker portrait on tiles, sentence-case smaller section headings, hairline rules instead of 2px, borderless search with an underline, 1.5px borders elsewhere, no double-width tiles on phones. Approved and merged in v2.7.
- **2026-09-05 (v2.6 — mobile pass + icons)** — User: mobile was cluttered. Header letters P/C/W/S/↑ are now **icons** (person, cart, red heart, ₹ for Sell, arrow) at every size. **Phones (≤600px) only**: top bar is just the name tile + search; a **bottom navigation bar** (Home · Categories · Cart · Wishlist · Profile, with badges) replaces it; **Categories opens a bottom sheet** (the capsule repositioned; closes on choice or tap outside); everything is scaled down (10px page padding, 2-column square tiles with smaller captions, smaller featured tile/type/buttons/inputs, modal and cart rows compacted); theme button and toasts sit above the nav. Desktop/ultrawide untouched apart from the icons. Verified with 390px frames (home, categories sheet, cart) and a 1920px header check.
- **2026-09-05 (plan v2)** — User asked to plan the real build with all AI features. Decisions: **Python FastAPI + PostgreSQL backend; the approved prototype becomes the frontend** (vanilla JS split into ES modules calling the JSON API); the Next.js app is dropped, its schema/contracts/API shapes ported. Hosting via the **GitHub Student Pack**: **Heroku** Basic dyno + Postgres Essential-0 ($12 of the $13/mo credit, always on), GitHub Pages for the frontend, a **Name.com** domain (free 1 yr; try kalasutra.in/.co/.app). Uploads stored in Postgres (bytea) — no third service, no card. **Hosted AI APIs** (fal.ai cutout, Voyage embeddings), Sarvam for language, Claude for structure/vision/pricing; **Sarvam key available now, others later** → every AI route has fixture/record/live modes. Full plan in `plan.md` (the old Next.js plan is `plan-v1-nextjs.md`). Build order: backend skeleton → frontend split + buyer wiring → artisan side → deploy → AI in fixture mode → go live per key → polish/rehearsal → Android later. **Not started yet; awaiting the user's go and three actions from them (claim Heroku offer + `heroku login`, claim the Name.com domain, paste the Sarvam key into `backend/.env`).**
- **2026-09-05 (deployed)** — User's Vercel import showed 404 (no root `index.html`). Added `index.html` (meta-refresh to the prototype), `prototype/index.html`, and `vercel.json` (redirect `/` → `/prototype/kalasutra-prototype.html`). Enabled **GitHub Pages** on `main` via the API: live at **https://dhruvjoshi-git.github.io/kalasutra/** (build verified, prototype + images return 200). Vercel redeploys on push if the repo is connected there.
- **2026-09-05 (v2.5 — renamed to KalaSutra, public repo)** — User renamed the product **KalaSetu → KalaSutra** (prototype logo/title/footer/support email, docs, design files; the prototype file is now `prototype/kalasutra-prototype.html`; the live app's seed logins still use `@kalasetu.in` until the app is restructured). Created the **public GitHub repo `DhruvJoshi-GIT/kalasutra`** through the account's stored Git credential (scope `repo`), with `C:/swadesh` as the repo root: `.gitignore` excludes `tools/ pgdata/ pickindian/ scratch/ *.log`; `README.md` documents the project, the prototype flow and the design system. Commits are authored as the user only (`Dhruv Joshi <87961604+DhruvJoshi-GIT@users.noreply.github.com>`, no co-author trailer — user's explicit wish so their avatar shows on every commit). Standing rule 3 now reads: commit/push to this repo when the user asks; never touch `pickindian` remotes.
- **2026-09-05 (v2.4 — hero removed)** — User: drop the hero page; the site opens straight on the shop. The featured tile is now a **slider of 8 products** (`FEATURED[]`, `.ftrack` translateX, ‹ › + dots on the photo, auto-advance 5s, pauses on hover, arrows/dots stop propagation so they don't open the product). The **logo** carries the name: at the top of the page the K tile stretches into a rounded "KalaSutra" tile; once scrolled (>40px) it shrinks back to the square K (`paintTop()` toggles `.logo.wide` on scroll). The ↑ button is plain "Top" again. All hero code, `?nohero`, and `sessionStorage['ks-hero']` are gone. Verified at 1920 / 3440 / 390 (slide 1 and slide 3, top and scrolled).
- **2026-09-05 (v2.3 — the "five things")** — I reviewed the site as customer / artisan / developer and the user asked for the top five: (1) **Featured story tile** at the top of the home page instead of "On sale": one product per week (rotates through `FEATURED[]` by week number) with the maker's portrait, craft, place, a line of their story, price, "See the piece" (opens the popup) and "Meet the maker". (2) **Rhythm**: every 7th tile is double-width (`.card.wide`, `grid-auto-flow:dense`, photo absolutely filled so it never changes the row height). (3) **Tile caption**: tiny round maker portrait + name · place, product name, one **craft word** (`CRAFT{}` per product, e.g. "Kanjivaram silk", "Gota patti"), price bold and larger; discount is a quiet grey strikethrough only — the green "% off" badge is gone from the popup too. (4) **Each product shown once** (the "On sale" grid is removed) and a **sort control** (Newest · Price low→high · Price high→low · Discounted first · Name A–Z) in the section header; search also matches the craft word and Hindi name. (5) **Ultrawide**: above 2200px tiles have a 360px minimum and slightly larger type, so wide screens get bigger photos rather than more of them; the featured photo is capped at 62vh. Verified by screenshots at 390 / 1920 / 3440. **The user said they will approve before we move on to the live site.**
- **2026-09-05 (v2.2)** — (1) **Seller login scales with the screen** (user: not adapting on the 3440 monitor): the two panels now stretch to the full viewport height (was `align-items:start` from `.split`), hatched panel fills, heading/inputs/button/illustration use `clamp()`/vw so they grow on ultrawide and the illustration shrinks (`flex:1;min-height:0;max-height:40vh`) so nothing falls below the fold. (2) **Wishlist rows open the product popup** on click (thumb + name are one link; panel closes); + Cart / ✕ remain. Verified with screenshots at 1920 and 3440.
- **2026-09-05 (v2.1)** — (1) **High-res photos**: the user supplied the 25 original files in `C:/swadesh/photo/files/` (jpg/webp, up to 2000px). Converted to progressive JPEGs ≤1600px under the same names in `prototype/img/` (≈4 MB total); the jute-box shot had a shop logo cropped off the corner. The old collage crops are gone. Tiles/modal/hero use `object-position` so faces stay in frame on portrait shots. Hero slides now use the sharpest files (saree-green, bangles-rack, dolls, bead-earrings, drawers). (2) **Adaptive grid**: user reported one tile per row after expanding the capsule; fixed by dropping the fixed 4/5 columns — `.grid.shop` is now `repeat(auto-fill, minmax(260px,1fr))` (200px under 1100px, 2 columns under 600px), so the count follows the width and the capsule just removes a column (≈5 open / 6 closed at 1920, 3 at 1280). (3) **↑ button**: reads "Top" when scrolled down (scrolls to top); at the top it reads "Hero" and clicking brings the splash back (`buildHero(true)`), which can then be dismissed again as usual.
- **2026-09-05 (v2 rebuild)** — User gave the full flow spec; `prototype/kalasutra-prototype.html` rewritten from scratch (old file archived at `prototype/archive/kalasutra-prototype-v1-2026-09-05.html`). What it now is: **one-way hero** (fixed splash with the photo slider; scroll/swipe/key/click dismisses it, `sessionStorage['ks-hero']` keeps it gone for the session, no way back); sticky header with **rounded-square K**, pill search that filters live, **P / C / W / S / ↑** rounded buttons, **C and W carry count badges**; **floating category capsule** on the left (rounded, collapsible ‹/›, state in `ks-side`; expanded = 4 tiles per row, collapsed = 5, 6/8 above 2200px, auto-fill below 1100px, 2 on phones, horizontal chip strip under 900px); the three filter dots are gone; **categories filter for real** (All · Sarees & textiles · Menswear · Jewellery · Footwear · Home & decor · Art & craft · Toys & games — taken from what is in the photos); 25 products, all photos used. **Product = modal popup** (no product page, no Related grid): gallery, title, Hindi name, maker link, stars, price/discount, qty, Add to cart / Buy now / Wishlist, then Details, Shipping & returns (policy text), Reviews (star picker + form), Comments & questions (form) — reviews/comments are stored locally, empty until someone writes one. **Cart is functional** (`ks-cart` in localStorage): qty ±, remove, subtotal, shipping rule (free ≥ ₹999 else ₹79), small horizontal "You may also like" strip, and **checkout inline**: choose/add address, choose/add UPI or card (validated), Place order → order stored → Order-confirmed page. **Account**: name/phone/email, Address / Payment / Your orders accordions — **no demo data**, empty states until the user adds something; orders appear only after a real checkout. **Wishlist** works (♥ on tiles, panel with add-to-cart/remove). Seller side kept (login → my products → add product with checklist; a submitted listing is stored in `ks-extra` and shows in the shop). Artist pages per maker (`#artist/<key>`). **All boxes/buttons/inputs/tiles are rounded** (`--r:14px`, tiles 12px, capsule 18px). The bottom screen-map bar is removed; navigation is the site's own. Testing switch: `?nohero` in the URL skips the splash. Verified via headless Edge screenshots: hero, shop (open/closed capsule), category filter, modal, cart (empty checkout + ready checkout), account (with and without data), 3440px, and 390px phone frames for shop/modal/cart.
- **2026-09-05 (later)** — Second round of prototype feedback, all done in `prototype/kalasutra-prototype.html`: (1) **hero** — centre box is now translucent (74% paper + blur) over a photo/video **slider** behind it (5 slides, auto-advance 4.5s, ‹ › + dots, Ken Burns drift; list is `HERO[]` at the top of the script — `{type:'video',src:'img/x.mp4'}` works; current slides are crops from the collages so they look soft at full screen until real hero photos/videos are dropped in); (2) **responsive** — app is full-width (no 1280px cap), sidebar/paddings/aside columns are `clamp()`-fluid, product grids use `auto-fill` (≈3 columns at 1920, 5 at 3440, 1 on phones), all two-column screens stack under 900px, bar wraps the search under 700px, category sidebar becomes a horizontal strip on phones; (3) **real photos** — the two collages in `C:/swadesh/photo/` were cropped with Pillow into 25 JPGs in `prototype/img/` (552 KB); product data `P[]` rewritten to 20 real items matching the photos (sarees, Pattachitra plate, Warli planter, marble vases, cane baskets, jewellery, juttis, kolhapuris…), `Jewellery` category added, all SVG placeholder patterns removed. Verified with headless Edge screenshots at 390 / 1920 / 3440 px for every screen.
- **2026-09-05** — User reviewed the prototype: "good", one change — product grids were cluttered (4–5 per row, touching). Fixed in `prototype/kalasutra-prototype.html`: every product grid (home On sale/Recommended, listing, seller products, artist Work) is now **3 per row** with 28px gaps + padding, each card has its own 1px border and a neo lift on hover; Related on the product page stays 2×2 but spaced; 2 per row under 1180px. No other feedback yet on S / ↑ icons.
- **2026-09-04 19:45** — `progress.md` rewritten as a clean handoff at the user's request.
- **2026-09-04 19:40** — Clickable prototype of the user's hand-drawn vision written to `C:/swadesh/prototype/kalasutra-prototype.html`; all screens dry-run rendered.
- **2026-09-04 19:20** — Windows killed the dev server + Postgres (low memory); both restarted, site verified.
- **2026-09-04 19:00** — Design direction B ported to the live site with PrithviNet interactions and dark mode. tsc + lint clean; all storefront + portal pages 200 as Priya. A strip script wrecked indentation in 55 files; fixed with Prettier.
- **2026-09-04 18:30** — User: no more Artifacts; review via local HTML / localhost only. Design canvases deleted by the user.
- **2026-09-04 18:10** — User picked direction B. Full website design canvas produced (8 boards + 2 reference sketches).
- **2026-09-04 17:30** — First design canvas (three directions, then a folk-painted board after a reference image).
- **2026-09-04 16:50** — Verified via curl as Priya: product create → visible in shop → PDP → PATCH → DELETE; order PENDING→CONFIRMED; ownership check (Meera → 404); upload round-trip. tsc + eslint clean.
- **2026-09-04 16:40** — Phase 1 artisan portal built: schema extension, role-aware auth + middleware, local storage + upload, artisan APIs, `/sell`, `/artisan` dashboard/products/orders/profile+KYC. Seed extended with 3 artisans + 4 orders.
- **2026-09-04 16:05** — Node 24.19 installed; Postgres 17.11 portable initialised; `.env`; `db:push` + `db:seed` green; storefront verified. Fixed pre-existing `next/image` host bug.
- **2026-09-04 15:50** — Repo audited (`findings.md`). Plan agreed. Knowledge base created. Decided: portable Postgres instead of Docker; in-process kNN instead of pgvector.
