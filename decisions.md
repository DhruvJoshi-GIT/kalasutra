# Decisions log (append-only)

Format: date · decision · why · what it rules out.

- **2026-09-04 · Reuse `pickindian` as the base, do not restart.** It already has the buyer storefront, Prisma schema, auth, UI kit and warm craft palette. Rules out: a fresh scaffold, a different framework.
- **2026-09-04 · Build order: website → key AI features → Android app.** User's call. The artisan portal is built on the web first so every AI feature is exercised through a browser before the mobile shell exists. Rules out: starting with Expo.
- **2026-09-04 · Sarvam AI is the language provider** (Saarika/Saaras ASR, Bulbul TTS, Mayura translation). 1,000 free credits, Indian model, covers all mainstream Indian languages. Claude (`claude-opus-5`) handles only what Sarvam cannot: schema-constrained listing JSON, photo QC via vision, pricing reasoning. Rules out: Whisper, Google STT, Claude generating Hindi copy directly.
- **2026-09-04 · Sarvam credit discipline is phase-0 work.** One wrapper (`src/lib/ai/sarvam.ts`), `SARVAM_FIXTURES=1` replay mode for dev, content-hash cache so nothing is billed twice. Rules out: iterating UI against the live API.
- **2026-09-04 · Cloud AI APIs are allowed** (user confirmed). The PRD's "all services local" constraint is relaxed for AI only; DB and storage stay local.
- **2026-09-04 · Mobile auth = phone OTP + JWT, separate from NextAuth.** Artisans have phones, not emails; NextAuth v5 fights React Native. Web NextAuth untouched. Rules out: NextAuth on the mobile app.
- **2026-09-04 · No agent memory; `D:\swadesh` is the only knowledge base.** User instruction. `progress.md` is the handoff file, updated after every significant execution.
- **2026-09-04 · Node installed system-wide via winget; PostgreSQL run from portable EDB zip binaries under `D:\swadesh\tools\pgsql` with data in `D:\swadesh\pgdata`.** Docker Desktop on Windows Home needs WSL2 + reboot — not worth the risk mid-hackathon. Portable Postgres needs no admin, no service, and is deleted by removing the folder. Rules out: Docker for the DB (compose file can still be added later for judges / other machines).
- **2026-09-04 · Embeddings stored as `Float[]` on Product; kNN computed in Node, not pgvector.** pgvector has no easy Windows build and the catalogue is ~500 rows × 1024 dims — in-process cosine over that is sub-millisecond. Rules out: pgvector for now (revisit if the catalogue passes ~20k rows).
- **2026-09-04 · Synthetic catalogue rows are flagged `isSynthetic = true` and disclosed.** Undisclosed synthetic data is a judging risk; disclosed + anchored to the 13 real seed SKUs reads as rigour.
- **2026-09-04 · Uploads go to local disk (`<repo>/storage`, gitignored), served by `GET /api/files/<key>`.** No MinIO/S3 for the website phase — one less moving part, and `src/lib/storage/index.ts` is a three-function seam that swaps to S3/R2 without touching callers. Rules out: MinIO in phase 1.
- **2026-09-04 · Artisans sign in on the web with the existing NextAuth credentials flow; the `Role` enum gained `ARTISAN` and `BUYER`, and the JWT now carries `role`.** `/artisan/*` is protected in `src/middleware.ts` (no session → login; wrong role → `/sell`). Phone-OTP + JWT is still the plan for the mobile app only.
- **2026-09-04 · `/sell` is the single onboarding page** — signed-out visitors create account + profile in one form (`POST /api/auth/signup` with `role: 'ARTISAN'`); signed-in buyers upgrade in place (`POST /api/artisan/profile` promotes them, then `useSession().update()` refreshes the JWT). Rules out: a separate artisan signup page.
- **2026-09-04 · Multi-vendor orders keep order-level status (PRD US 1.4) but the artisan API filters line items to their own products and reports their own line total.** Per-item fulfilment status is a post-hackathon refinement.
- **2026-09-04 · Products that were ever ordered are deactivated, never deleted** (`DELETE /api/artisan/products/:id`), so order history stays intact.
- **2026-09-04 · No more Artifacts.** User instruction: do not publish design canvases or pages as Artifacts; show all work as local HTML files or on localhost for review. Rules out: the Artifact tool for this project.
- **2026-09-04 · Interaction language ported from the user's own repo `DhruvJoshi-GIT/PrithviNet`** (behaviour only, not its visuals): hard offset shadow `3px 3px 0` that lifts to `4px` + `translate(-1,-1)` on hover and presses flat (`translate(3,3)`, shadow 0) on click (`.neo`); cards lift to `5px` (`.neo-card`); nav rows nudge `translate(2,2)` on press (`.neo-press`); inputs grow a marigold offset shadow on focus; theme toggle uses `document.startViewTransition` with a circle reveal from the fixed bottom-right toggle (`::view-transition-new(root)` clip-path animation, 0.9s), theme stored in `localStorage['ks-theme']`, `.dark` class on `<html>`, anti-flash inline script in `<head>`. Light (paper) is the default.
- **2026-09-05 · Real build = Python FastAPI + PostgreSQL backend, the approved prototype as the frontend (plain scripts, no framework), Expo Android later on the same API.** User's call after the prototype was approved; Python is the natural home for the AI work and the prototype is already the UI. Rules out: the Next.js app (dropped; its schema/contracts/ownership rules were ported), a frontend rewrite in React.
- **2026-09-05 · Frontend modules are classic `<script>` files loaded in order, not ES modules.** Every inline `onclick` in the prototype keeps working with zero bridge code and the site still opens from `file://`. Rules out: a bundler, `type="module"`.
- **2026-09-05 · Integer primary keys; uploads stored in Postgres (`stored_file` bytea); JWT bearer tokens in localStorage; guest cart/wishlist merge on sign-in.** Integer ids keep the prototype's inline handlers untouched; bytea removes a third hosting service; bearer tokens avoid cross-site cookie/CORS pain between the static site and the API.
- **2026-09-05 · Hosting: Render (free web service) + Neon (free Postgres); GitHub Pages for the site; domain kalasutra.live (Name.com student offer).** Heroku's student offer could not be claimed; Render's own free Postgres expires after 30 days, Cloudflare R2 needs a card, DigitalOcean's credit ended Aug 2026. Rules out: Heroku, Render Postgres, R2.
- **2026-09-05 · AI: hosted models only (fal.ai cutout, Voyage embeddings), Sarvam for language, Claude for structure/vision/pricing; every provider has fixture / record / live modes.** Free-tier RAM cannot run local ML; the Sarvam key exists today and the others do not, so the whole seller flow must run from fixtures. Rules out: rembg / sentence-transformers on the server.
- **2026-09-05 · Nothing is pushed until the user says "publish" (local commits are fine).** User instruction after a change went live without review. Exception made explicitly when the user asked to move the site to the domain.
- **2026-09-08 · Push every change to GitHub as it is made.** User instruction ("make the changes simultaneously to the GitHub repo as well"). Supersedes the 2026-09-05 "push only on publish" rule. Rules out: batching local commits for review first.
- **2026-09-08 · Brand = the user's flame-क mark + bilingual wordmark कलाSutra, rendered as text.** The mark is cut from the supplied JPEG into a transparent PNG (`web/img/logo-mark.png`, inverted with a CSS filter in dark mode); the wordmark is live text (Noto Sans Devanagari + Archivo) so it stays crisp, selectable and theme-aware. Header: mark + name at the top of the page, mark alone once scrolled. Rules out: a raster wordmark, the old "K" tile.
- **2026-09-08 · Repo root redirects to `web/`; Vercel dropped.** `index.html` at the root meta-refreshes to `web/` so kalasutra.live opens the real site (it used to open the prototype); `vercel.json` removed and the user deletes the Vercel project. Rules out: moving `web/` to the repo root (the backend's `SERVE_WEB` mount and `config.js` host mapping would have to change for no gain).
- **2026-09-08 · Demo mode in the frontend when the API is unreachable.** `web/catalogue.json` (snapshot of `/catalogue/bootstrap`) + `web/js/demo.js` (simulated auth, cart, addresses, payments, orders, reviews, comments in `localStorage['ks-demo']`). Chosen because the public site had to work before Render/Neon exist and must keep working offline at the venue. The real API takes over automatically when reachable. Rules out: showing an error page until the backend is deployed; a mock server.
- **2026-09-08 · Mobile Categories sheet toggles in place (no bottom-bar re-render on tap).** Re-rendering detached the tapped link before the outside-click handler ran and closed the sheet immediately. Rules out: re-rendering the bottom bar inside the toggle.
- **2026-09-09 · The repo now lives at `C:\swadesh` (was `D:\swadesh`).** Same layout; portable Postgres at `C:\swadesh\tools\pgsql`, data in `C:\swadesh\pgdata`, backend venv at `backend\.venv`. Every path in the docs was updated. Rules out: any remaining `D:` path.
- **2026-09-09 · Product photos come from the user's zip batch (`photo/new photos/*.zip`).** Six existing listings got their photo replaced (Bagru saree, navy Banarasi, green Kanjivaram, grey kurta, silver jhumkas, phulkari jutti), 57 listings were added (25 → 82), three makers were added at the end of the makers list (Khurja Pottery Collective, Jaipur Kundan House, Palghar Adivasi Kala; 11 → 14, so the demo OTP numbers of the old makers did not move), and a `pottery` category was added. Photos are re-encoded to ≤1400 px JPEG in `web/img/` with 520 px thumbnails in `web/img/t/`; brand badges on three saree photos were patched out. Rules out: committing the zips (133 MB of generated PNGs) — the web JPEGs are the canonical copies.
- **2026-09-09 · Opening slider = seven category banners shown whole.** The banners have baked-in text, so the slider box keeps their own 3.2:1 ratio (`aspect-ratio` + `object-fit:contain`) and the banners were edge-extended in Pillow to exactly 3.2:1, so nothing is ever cropped on a phone, tablet or ultrawide; each banner links to its category; swipe works on touch. Rules out: the product-story slider, `object-fit:cover`, fixed heights.
- **2026-09-09 · Tiles load thumbnails; the popup shows the whole photo.** `pic(p)` maps `img/x.jpg` → `img/t/x.jpg` for tiles (square crop), `pic(p,true)` in the popup uses the full file with `object-fit:contain`. Rules out: 82 full-size photos on the home page.
- **2026-09-09 · Every two-column screen stacks under 900 px with `!important`.** Inline `grid-template-columns` on the upload, artist and login screens used to beat the media query — that was the "crammed" phone layout. Cart rows, popup buttons, the wishlist panel and the header have explicit phone (≤600 px) and tiny-screen (≤340 px, ≤290 px) rules. Rules out: inline column definitions without a phone override.
- **2026-09-09 · Photo studio (sellers only) runs in three tiers, so it always finishes:** (1) server cutout on the KalaSutra API through fal.ai BiRefNet when `FAL_KEY` is set (`POST /api/ai/cutout`, `POST /api/ai/enhance`); (2) otherwise an on-device model in the browser (`@imgly/background-removal` from jsDelivr, ~45 MB downloaded once and cached, nothing uploaded); (3) otherwise a "framed" version (photo on the paper backdrop). Backdrops (paper, plain, warm, terracotta wall, indigo block print, haveli sandstone, marigold, ink) and the contact shadow are drawn on a canvas in the browser and with Pillow on the server, in step. `FAL_KEY` is the only thing the user has to add. Rules out: local ML on the Render box; waiting for a key before shipping the studio.
- **2026-09-09 · Seller listings go through the same `POST /api/artisan/products` in both modes.** Online: the studio JPEG (data URL) is stored in Postgres (`stored_file`) and served from `/api/files/{key}`; the product is live immediately. Demo mode: `js/demo.js` keeps the listing in `localStorage['ks-demo'].listings` and merges it into the catalogue on every load. Rules out: a separate demo-only code path in the seller screens.
- **2026-09-09 · Provider mode resolution: `<NAME>_MODE`, else live when that provider's key is set, else `AI_MODE`.** Pasting a key is enough to go live; a live mode without a key silently becomes fixture. Rules out: a second switch to flip after adding a key.
- **2026-09-09 · Testing = Playwright on the installed Edge (`channel="msedge"`, no browser download).** `scratch/shoot.py` screenshots every screen at 7 viewports (280 → 3440 px) and flags JS errors and horizontal overflow; `scratch/live_click.py` clicks tiles with a real mouse/touch on the live site; `scratch/studio_e2e.py` runs the studio + listing flow. Rules out: judging layouts from code alone.
- **2026-09-09 · HTTPS enforced on kalasutra.live.** The certificate never appeared after the domain was set on 2026-09-05; removing and re-adding the custom domain through the Pages API made GitHub issue it within minutes, and `https_enforced` was then switched on. Rules out: waiting on a stuck certificate — re-add the domain instead.
- **2026-09-09 · A document-level `submit` listener cancels every native form submit.** All forms are handled by async functions via `onsubmit="return fn(this)"`; an async function returns a Promise (truthy), so the browser also submitted the form natively and reloaded the page as `?phone=…` — that is why the seller OTP code step never appeared on the live site and the buyer sign-in reloaded. Verified through the real forms with Playwright (seller OTP → shop → studio → listing; buyer sign-in → address → UPI → order → review) at desktop and phone sizes. Rules out: relying on the return value of an inline async handler.
